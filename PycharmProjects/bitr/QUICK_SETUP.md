# Быстрая настройка системы

## ⚡ Быстрый старт (5 минут)

### 1. Настройте вебхук в Bitrix24

1. Откройте: **Разработчикам** → **Входящий вебхук**
2. Скопируйте URL вебхука (например: `https://karkas.bitrix24.ru/rest/153/vqukf1eoovd3ba99/`)
3. Настройте права:
   - ✅ `crm` - доступ к CRM
   - ✅ `crm.contact` - работа с контактами  
   - ✅ `crm.deal` - работа со сделками
4. Нажмите **"СОХРАНИТЬ"**

### 2. Обновите `.env` файл

```bash
BITRIX_WEBHOOK_URL=https://karkas.bitrix24.ru/rest/153/vqukf1eoovd3ba99
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа_случайная_строка
```

### 3. Перезапустите контейнеры

```bash
docker-compose restart backend frontend
```

### 4. Проверьте работу

```bash
# Проверьте логи
docker-compose logs backend --tail 20

# Проверьте подключение к Bitrix24
curl http://localhost/api/admin/bitrix/test
```

## ✅ Готово!

Теперь система:
- ✅ Получает полные данные из Bitrix24
- ✅ Проверяет пользователей в Bitrix24
- ✅ Обновляет Bitrix24 при оплате
- ✅ Использует JWT для аутентификации

## 🔐 Как войти

### Для клиентов (Magic Link):

1. Запросите magic link:
```bash
curl -X POST "http://localhost/api/auth/magic-link?email=ваш_email@example.com"
```

2. Используйте полученную ссылку для входа

3. Токен будет сохранен и использоваться для всех запросов

### Для админов:

Используйте тот же magic link или создайте отдельную систему с паролями (см. `AUTHENTICATION_GUIDE.md`)

## 📋 Чеклист

- [ ] Вебхук создан и права настроены
- [ ] URL вебхука в `.env`
- [ ] JWT_SECRET в `.env`
- [ ] Контейнеры перезапущены
- [ ] Проверка работы пройдена

## ⚠️ Важно

1. **Права вебхука** - должны включать доступ к CRM, контактам и сделкам
2. **Пользователи в Bitrix24** - должны существовать контакты с email
3. **Поле UF_PAID_AMOUNT** - должно существовать в Bitrix24 (или данные будут только в БД)

## 🆘 Проблемы?

См. `SETUP_GUIDE.md` для подробной инструкции и решения проблем.

