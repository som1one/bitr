# Система управления рассрочкой

Полнофункциональная система для управления рассрочками с интеграцией Bitrix24 и YooKassa.

## 📋 Содержание

- [Как работает система](#как-работает-система)
- [Архитектура](#архитектура)
- [Как проверить работу](#как-проверить-работу)
- [Настройка](#настройка)
- [API Endpoints](#api-endpoints)
- [Безопасность](#безопасность)
- [Структура данных](#структура-данных)
- [Решение проблем](#решение-проблем)
- [Логирование](#логирование)

---

## 🚀 Как работает система

### 1. Пользователь входит в систему

- Открывает страницу `/auth/login`
- Вводит свой email адрес
- Система проверяет наличие рассрочки в Bitrix24 для данного email
- Если рассрочка найдена → создается magic link → пользователь авторизуется через JWT токен
- Если рассрочки нет → показывается ошибка "Пользователь не найден"

### 2. Просмотр рассрочки

- Пользователь видит свою рассрочку на странице `/installment`
- Данные получаются из Bitrix24 и локальной БД (объединение данных)
- Отображается:
  - Общая сумма рассрочки
  - Оплаченная сумма
  - Остаток к оплате
  - График платежей
  - Прогресс оплаты (график)

### 3. Оплата

- Пользователь нажимает кнопку "Оплатить"
- Создается платеж в YooKassa
- Пользователь перенаправляется на страницу оплаты YooKassa
- После успешной оплаты приходит webhook от YooKassa
- Система обновляет:
  - Локальную БД (увеличивает `paid_amount`)
  - Bitrix24 (обновляет `UF_PAID_AMOUNT`)
  - Создает запись в `payment_logs`

### 4. Админ-панель

- Администратор видит все рассрочки на странице `/admin/deals`
- Может вносить наличные платежи через интерфейс
- Может изменять настройки рассрочки (срок, сумма, email, название)
- Может просматривать детали каждой рассрочки

---

## 🏗️ Архитектура

### Backend (FastAPI)

**Аутентификация:**
- `POST /auth/magic-link` - запрос magic link для входа
- `GET /auth/verify` - проверка JWT токена

**Рассрочка:**
- `GET /api/installment/my` - получение данных о рассрочке текущего пользователя

**Платежи:**
- `POST /api/payments/create` - создание платежа через YooKassa
- `POST /api/payments/webhook` - обработка webhook от YooKassa
- `GET /api/payments/logs` - получение логов платежей (для админа)

**Админка:**
- `GET /api/admin/deals` - список всех рассрочек
- `GET /api/admin/deals/{id}` - детали рассрочки
- `GET /api/admin/deals/export` - экспорт всех рассрочек с полными данными
- `GET /api/admin/bitrix/test` - тестовый endpoint для проверки данных из Bitrix24
- `POST /api/admin/deals/{id}/cash-payment` - внесение наличного платежа
- `PUT /api/admin/deals/{id}/settings` - изменение настроек рассрочки

### Frontend (Next.js)

**Публичные страницы:**
- `/auth/login` - страница входа (запрос magic link)
- `/auth/magic` - обработка magic link (автоматическая авторизация)

**Защищенные страницы (требуют авторизации):**
- `/installment` - страница рассрочки пользователя
- `/admin/deals` - список рассрочек (админка)
- `/admin/deals/[id]` - детали рассрочки (админка)

### База данных (PostgreSQL)

**Таблица `deals`:**
- `id` - первичный ключ
- `deal_id` - ID сделки из Bitrix24 (уникальный)
- `title` - название рассрочки
- `email` - email пользователя (индексируется)
- `total_amount` - общая сумма рассрочки
- `paid_amount` - оплаченная сумма (обновляется при платежах)
- `term_months` - срок рассрочки в месяцах
- `created_at` - дата создания записи
- `updated_at` - дата последнего обновления

**Таблица `payment_logs`:**
- `id` - первичный ключ
- `deal_id` - ID сделки из Bitrix24 (индексируется)
- `payment_id` - уникальный ID платежа (из YooKassa или генерируется для наличных)
- `amount` - сумма платежа
- `status` - статус платежа: `pending`, `paid`, `failed`
- `source` - источник платежа: `yookassa`, `admin_cash`
- `created_at` - дата создания записи

### Интеграции

**Bitrix24:**
- Источник данных о рассрочках (сделки типа "Рассрочка")
- Используется для проверки существования пользователя
- Обновляется при платежах (поле `UF_PAID_AMOUNT`)
- API методы: `crm.deal.list`, `crm.deal.get`, `crm.deal.update`, `crm.contact.list`

**YooKassa:**
- Платежный шлюз для онлайн-платежей
- Создание платежей через API
- Обработка webhook при успешной оплате

---

## ✅ Как проверить работу

### 1. Проверка подключения к Bitrix24

```bash
# Проверка через API (требует авторизации)
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost/api/admin/bitrix/test

# Или через Python команду в контейнере
docker-compose exec backend python -c "from bitrix.client import get_all_installment_deals; deals = get_all_installment_deals(); print(f'Найдено сделок: {len(deals)}')"
```

### 2. Проверка аутентификации

```bash
# Запрос magic link (email через query параметр)
curl -X POST "http://localhost/auth/magic-link?email=ваш_email@example.com"

# Или через body JSON
curl -X POST "http://localhost/auth/magic-link" \
  -H "Content-Type: application/json" \
  -d '{"email": "ваш_email@example.com"}'

# Проверка токена (после получения magic link)
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost/auth/verify?token=YOUR_TOKEN
```

### 3. Проверка получения рассрочки

```bash
# С токеном (после авторизации)
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost/api/installment/my
```

### 4. Проверка создания платежа

```bash
curl -X POST \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"amount": 10000}' \
  http://localhost/api/payments/create
```

### 5. Проверка админки

```bash
# Список всех рассрочек
curl -H "Authorization: Bearer ADMIN_TOKEN" http://localhost/api/admin/deals

# Детали конкретной рассрочки
curl -H "Authorization: Bearer ADMIN_TOKEN" http://localhost/api/admin/deals/123

# Экспорт всех рассрочек
curl -H "Authorization: Bearer ADMIN_TOKEN" http://localhost/api/admin/deals/export
```

### 6. Проверка через браузер

1. **Откройте:** `http://localhost/auth/login`
2. **Введите email** пользователя с рассрочкой в Bitrix24
3. **Нажмите "Получить ссылку для входа"**
4. **Перейдите по magic link** (в development режиме ссылка показывается на странице)
5. **Должны увидеть** страницу с рассрочкой на `/installment`

---

## 🔧 Настройка

### 1. Переменные окружения (`.env` файл)

Создайте файл `.env` в корне проекта со следующим содержимым:

```bash
# Bitrix24
BITRIX_WEBHOOK_URL=https://your-domain.bitrix24.ru/rest/1/webhook_code

# YooKassa
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET=your_secret_key

# Frontend
FRONTEND_URL=http://localhost

# JWT
JWT_SECRET=your_secret_key_min_32_chars_long_for_security

# База данных (для PostgreSQL)
DATABASE_URL=postgresql://user:password@db:5432/installment_db

# Окружение
ENVIRONMENT=development  # development или production
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR

# Проверка подписи webhook (в production обязательно true)
VERIFY_WEBHOOK_SIGNATURE=false  # В development можно false, в production должно быть true

# CORS (опционально, через запятую для нескольких доменов)
ALLOWED_ORIGINS=http://localhost,https://your-domain.com
```

### 2. Запуск системы

**ВАЖНО: Перед запуском убедитесь, что Docker Desktop запущен!**

```bash
# 1. Проверьте, что Docker Desktop запущен
# В Windows: проверьте иконку Docker в трее или запустите Docker Desktop из меню Пуск

# 2. Проверьте статус Docker
docker ps

# 3. Запуск всех сервисов (backend, frontend, database, nginx)
docker-compose up -d

# 4. Проверьте, что все контейнеры запущены
docker-compose ps

# Должны быть запущены все 4 сервиса:
# - installment-db
# - installment-backend
# - installment-frontend
# - installment-nginx

# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f nginx
docker-compose logs -f db

# Перезапуск сервиса
docker-compose restart backend
docker-compose restart frontend

# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes (удалит БД!)
docker-compose down -v
```

**Если при запуске возникает ошибка `ERR_CONNECTION_REFUSED`:**
1. Убедитесь, что Docker Desktop запущен и работает
2. Проверьте статус контейнеров: `docker-compose ps`
3. Если контейнеры не запущены, запустите их: `docker-compose up -d`
4. Проверьте логи: `docker-compose logs nginx`

### 3. Настройка Bitrix24

1. **Создайте входящий вебхук в Bitrix24:**
   - Перейдите в настройки Bitrix24 → Разработчикам → Другие → Входящий вебхук
   - Нажмите "Добавить вебхук"
   - Укажите права доступа:
     - `crm` - доступ к CRM
     - `crm.contact` - работа с контактами
     - `crm.deal` - работа со сделками
     - `crm.deal.list` - получение списка сделок
     - `crm.deal.get` - получение данных сделки
     - `crm.deal.update` - обновление сделок
   - Скопируйте URL вебхука (выглядит как `https://your-domain.bitrix24.ru/rest/1/webhook_code`)
   - Вставьте в `.env` файл как `BITRIX_WEBHOOK_URL`

2. **Убедитесь, что в Bitrix24:**
   - Есть сделки с типом оплаты "Рассрочка"
   - У сделок заполнены поля `UF_TERM_MONTHS` (срок в месяцах) и `UF_PAID_AMOUNT` (оплаченная сумма)
   - У контактов указан email адрес
   - Сделки связаны с контактами

### 4. Настройка YooKassa

1. **Зарегистрируйтесь в YooKassa:**
   - Перейдите на https://yookassa.ru
   - Создайте магазин и получите `SHOP_ID` и `SECRET_KEY`

2. **Настройте webhook:**
   - В личном кабинете YooKassa перейдите в настройки магазина
   - Укажите URL для webhook: `http://your-domain/api/payments/webhook`
   - Включите уведомления о событиях платежей

3. **Скопируйте данные в `.env`:**
   - `YOOKASSA_SHOP_ID` - ID вашего магазина
   - `YOOKASSA_SECRET` - секретный ключ

---

## 📡 API Endpoints

### Аутентификация

#### `POST /auth/magic-link`

Запрос magic link для входа по email.

**Параметры:**
- Query: `email` (string, обязательный) - email пользователя
- Или Body JSON: `{"email": "user@example.com"}`

**Ответ:**
```json
{
  "ok": true,
  "message": "Ссылка для входа отправлена на user@example.com",
  "link": "http://localhost/auth/magic?token=..." // Только в development режиме
}
```

**Ошибки:**
- `400` - email не указан
- `404` - пользователь не найден в Bitrix24 или не имеет рассрочки

#### `GET /auth/verify`

Проверка JWT токена.

**Параметры:**
- Query: `token` (string, обязательный) - JWT токен

**Ответ:**
```json
{
  "valid": true,
  "email": "user@example.com",
  "token": "eyJ..."
}
```

**Ошибки:**
- `401` - токен неверный или истек

### Рассрочка

#### `GET /api/installment/my`

Получение данных о рассрочке текущего пользователя.

**Заголовки:**
- `Authorization: Bearer YOUR_TOKEN` (обязательный)

**Ответ:**
```json
{
  "deal": {
    "deal_id": "123",
    "title": "Рассрочка на покупку",
    "email": "user@example.com",
    "total_amount": 300000,
    "paid_amount": 100000,
    "remaining_amount": 200000,
    "term_months": 6,
    "paid_months_count": 2
  },
  "payments": [
    {
      "month": "Январь 2026",
      "date": "10.01.2026",
      "amount": 50000,
      "status": "paid",
      "paid_amount": 50000
    },
    {
      "month": "Февраль 2026",
      "date": "10.02.2026",
      "amount": 50000,
      "status": "paid",
      "paid_amount": 50000
    },
    {
      "month": "Март 2026",
      "date": "10.03.2026",
      "amount": 50000,
      "status": "pending",
      "paid_amount": 0
    }
  ]
}
```

**Ошибки:**
- `401` - не авторизован
- `403` - нет рассрочки
- `404` - рассрочка не найдена

### Платежи

#### `POST /api/payments/create`

Создание платежа через YooKassa.

**Заголовки:**
- `Authorization: Bearer YOUR_TOKEN` (обязательный)
- `Content-Type: application/json`

**Body:**
```json
{
  "amount": 10000
}
```

**Ответ:**
```json
{
  "url": "https://yookassa.ru/checkout/payments/..."
}
```

**Ошибки:**
- `400` - сумма должна быть больше 0, или сумма превышает остаток
- `401` - не авторизован
- `404` - рассрочка не найдена

#### `POST /api/payments/webhook`

Webhook endpoint для получения уведомлений от YooKassa.

**Заголовки:**
- `X-YooMoney-Signature` - подпись webhook (проверяется в production)

**Body:** JSON payload от YooKassa

**Ответ:**
```json
{
  "status": "ok"
}
```

#### `GET /api/payments/logs`

Получение логов платежей (для админа).

**Параметры:**
- Query: `deal_id` (string, опциональный) - фильтр по ID сделки

**Ответ:**
```json
[
  {
    "id": 1,
    "deal_id": "123",
    "payment_id": "yookassa_...",
    "amount": 10000,
    "status": "paid",
    "source": "yookassa",
    "created_at": "2026-01-10T12:00:00"
  }
]
```

### Админка

#### `GET /api/admin/deals`

Получение списка всех рассрочек.

**Заголовки:**
- `Authorization: Bearer ADMIN_TOKEN` (обязательный)

**Ответ:**
```json
[
  {
    "deal_id": "123",
    "title": "Рассрочка на покупку",
    "email": "user@example.com",
    "total_amount": 300000,
    "paid_amount": 100000,
    "remaining_amount": 200000,
    "term_months": 6,
    "status": "active",
    "contact_id": "456",
    "stage_id": "NEW",
    "date_create": "2026-01-01T00:00:00"
  }
]
```

#### `GET /api/admin/deals/{deal_id}`

Получение детальной информации о рассрочке.

**Заголовки:**
- `Authorization: Bearer ADMIN_TOKEN` (обязательный)

**Ответ:** аналогичен `/api/installment/my`

#### `GET /api/admin/deals/export`

Экспорт всех рассрочек с полными данными, включая график платежей.

**Заголовки:**
- `Authorization: Bearer ADMIN_TOKEN` (обязательный)

**Ответ:**
```json
{
  "stats": {
    "total_deals": 10,
    "in_db": 10,
    "paid": 2,
    "active": 7,
    "pending": 1,
    "total_amount": 3000000,
    "paid_amount": 500000,
    "remaining_amount": 2500000
  },
  "deals": [
    {
      "deal_id": "123",
      "title": "Рассрочка на покупку",
      "email": "user@example.com",
      "total_amount": 300000,
      "paid_amount": 100000,
      "remaining_amount": 200000,
      "term_months": 6,
      "paid_months": 2,
      "status": "active",
      "payments_count": 6,
      "payments": [...],
      "in_db": true
    }
  ]
}
```

#### `GET /api/admin/bitrix/test`

Тестовый endpoint для просмотра всех данных из Bitrix24.

**Заголовки:**
- `Authorization: Bearer ADMIN_TOKEN` (обязательный)

**Ответ:** подробная информация о всех сделках из Bitrix24 и локальной БД

#### `POST /api/admin/deals/{deal_id}/cash-payment`

Внесение наличного платежа.

**Заголовки:**
- `Authorization: Bearer ADMIN_TOKEN` (обязательный)
- `Content-Type: application/json`

**Body:**
```json
{
  "amount": 10000,
  "comment": "Оплата наличными в офисе" // опционально
}
```

**Ответ:**
```json
{
  "success": true,
  "deal_id": "123",
  "old_paid_amount": 100000,
  "new_paid_amount": 110000,
  "payment_amount": 10000
}
```

**Ошибки:**
- `400` - сумма должна быть больше 0, или сумма превышает остаток
- `409` - обнаружен дубликат платежа (защита от двойного нажатия)

#### `PUT /api/admin/deals/{deal_id}/settings`

Изменение настроек рассрочки.

**Заголовки:**
- `Authorization: Bearer ADMIN_TOKEN` (обязательный)
- `Content-Type: application/json`

**Body:**
```json
{
  "total_amount": 350000,  // опционально
  "term_months": 8,        // опционально
  "email": "new@example.com",  // опционально
  "title": "Новое название"    // опционально
}
```

**Ответ:**
```json
{
  "success": true,
  "deal_id": "123",
  "updated_fields": ["total_amount=350000", "term_months=8"],
  "deal": {
    "deal_id": "123",
    "title": "Новое название",
    "email": "new@example.com",
    "total_amount": 350000,
    "paid_amount": 100000,
    "term_months": 8
  }
}
```

---

## 🔐 Безопасность

### Аутентификация

- **JWT токены** с сроком действия **15 минут**
- Проверка пользователя в Bitrix24 перед входом (только пользователи с рассрочкой могут войти)
- Все защищенные endpoints требуют валидный JWT токен в заголовке `Authorization: Bearer TOKEN`
- Middleware на фронтенде автоматически перенаправляет неавторизованных пользователей на `/auth/login`

### Защита от ошибок

- **SELECT FOR UPDATE** для предотвращения race condition при одновременных платежах
- Валидация сумм платежей (не может быть отрицательной или превышать остаток)
- Проверка остатка перед созданием платежа
- Защита от дублирования платежей (проверка последних 30 секунд)
- Максимальный лимит суммы платежа (10 миллионов рублей)
- Логирование всех операций для аудита

### Webhook безопасность

- В production режиме проверяется подпись webhook от YooKassa (заголовок `X-YooMoney-Signature`)
- В development режиме проверка подписи может быть отключена через `VERIFY_WEBHOOK_SIGNATURE=false`

---

## 📊 Структура данных

### Deal (Рассрочка)

```json
{
  "deal_id": "123",
  "title": "Рассрочка на покупку",
  "email": "user@example.com",
  "total_amount": 300000,
  "paid_amount": 100000,
  "remaining_amount": 200000,
  "term_months": 6,
  "paid_months_count": 2,
  "status": "active"
}
```

**Статусы:**
- `active` - рассрочка активна, есть неоплаченные платежи
- `paid` - рассрочка полностью оплачена
- `pending` - рассрочка еще не оплачивалась

### Payment (Платеж)

```json
{
  "month": "Январь 2026",
  "date": "10.01.2026",
  "amount": 50000,
  "status": "paid",
  "paid_amount": 50000
}
```

**Статусы:**
- `paid` - платеж оплачен
- `pending` - платеж ожидает оплаты

---

## 🐛 Решение проблем

### Проблема: "Пользователь не найден в Bitrix24"

**Причины:**
- Контакт с таким email не существует в Bitrix24
- У контакта нет сделки с типом оплаты "Рассрочка"
- Неправильный email адрес

**Решение:**
1. Проверьте наличие контакта в Bitrix24 с указанным email
2. Убедитесь, что у контакта есть сделка типа "Рассрочка"
3. Проверьте права вебхука в Bitrix24 (должен иметь доступ к `crm.contact` и `crm.deal`)
4. Проверьте подключение к Bitrix24: `curl -H "Authorization: Bearer TOKEN" http://localhost/api/admin/bitrix/test`

### Проблема: "Ошибка 401/403"

**Причины:**
- JWT токен истек (срок действия 15 минут)
- Токен неверный или отсутствует
- Пользователь не имеет рассрочки в Bitrix24

**Решение:**
1. Запросите новый magic link через `/auth/login`
2. Убедитесь, что токен передается в заголовке: `Authorization: Bearer YOUR_TOKEN`
3. Проверьте, что пользователь существует в Bitrix24 с рассрочкой
4. Проверьте логи: `docker-compose logs backend | grep -i auth`

### Проблема: "Ошибка при создании платежа"

**Причины:**
- Сумма платежа превышает остаток по рассрочке
- Рассрочка уже полностью оплачена
- Неправильные настройки YooKassa

**Решение:**
1. Проверьте остаток по рассрочке через `/api/installment/my`

---

## 🚀 Деплой на чистый Ubuntu через Docker Hub

Ниже — самый простой способ: **сервер не билдит проект**, а просто тянет готовые образы из Docker Hub.

### 1) Подготовьте Docker Hub образы (на локальной машине)

Выберите имя репозитория (пример): `DOCKERHUB_REPO=yourname/bitr`

- **Backend**: `yourname/bitr-backend:latest`
- **Frontend**: `yourname/bitr-frontend:latest`

Сборка и пуш:

```bash
docker login
docker build -t yourname/bitr-backend:latest ./backend
docker build -t yourname/bitr-frontend:latest ./frontend
docker push yourname/bitr-backend:latest
docker push yourname/bitr-frontend:latest
```

### 2) Подготовьте сервер (Ubuntu 22.04+)

Установите Docker и docker compose plugin:

```bash
sudo apt update
sudo apt install -y ca-certificates curl gnupg
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo usermod -aG docker $USER
newgrp docker
```

Откройте порты:

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

### 3) Разверните проект на сервере

На сервере создайте папку:

```bash
mkdir -p ~/bitr && cd ~/bitr
mkdir -p nginx certbot/conf certbot/www
```

Скопируйте на сервер файлы из репозитория:
- `docker-compose.hub.yml`
- папку `nginx/` (минимум `nginx/ssl.conf`)
- файл `env.example` (как шаблон)

Создайте `.env`:

```bash
cp env.example .env
nano .env
```

Обязательно задайте:
- `DOMAIN`
- `FRONTEND_URL=https://$DOMAIN`
- `BITRIX_WEBHOOK_URL`
- `YOOKASSA_SHOP_ID` / `YOOKASSA_SECRET`
- `JWT_SECRET` (сгенерируйте: `openssl rand -hex 32`)
- `DATABASE_URL=postgresql://...@db:5432/...`

Также в `docker-compose.hub.yml` задайте `DOCKERHUB_REPO` через `.env`, например:

```env
DOCKERHUB_REPO=yourname/bitr
```

Запуск:

```bash
docker compose -f docker-compose.hub.yml pull
docker compose -f docker-compose.hub.yml up -d
docker compose -f docker-compose.hub.yml ps
```

### 4) HTTPS (Let’s Encrypt)

Файл `nginx/ssl.conf` содержит пути вида `/etc/letsencrypt/live/YOUR_DOMAIN/...`.
Замените `YOUR_DOMAIN` на ваш домен (или сделайте симлинк).

Первичная выдача сертификата (пример, замените домен и email):

```bash
docker compose -f docker-compose.hub.yml stop nginx
docker run --rm \
  -v $(pwd)/certbot/conf:/etc/letsencrypt \
  -v $(pwd)/certbot/www:/var/www/certbot \
  certbot/certbot certonly --standalone \
  -d YOUR_DOMAIN --agree-tos -m you@example.com --non-interactive
docker compose -f docker-compose.hub.yml up -d nginx
```

### 5) Быстрые проверки после деплоя

- Админ логин → `GET /api/admin/yookassa/check` (должно быть `ok: true` для ЮKassa)
- Bitrix test → `GET /api/admin/bitrix/test`

2. Убедитесь, что сумма платежа не превышает остаток
3. Проверьте настройки YooKassa в `.env` (`YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET`)
4. Проверьте логи: `docker-compose logs backend | grep -i payment`

### Проблема: "Webhook не обрабатывается"

**Причины:**
- Неправильный URL webhook в настройках YooKassa
- Проблемы с проверкой подписи (в production)
- Неправильная структура payload

**Решение:**
1. Проверьте URL webhook в настройках YooKassa: `http://your-domain/api/payments/webhook`
2. В development режиме установите `VERIFY_WEBHOOK_SIGNATURE=false` в `.env`
3. В production обязательно включите проверку подписи
4. Проверьте логи: `docker-compose logs backend | grep -i webhook`

### Проблема: "График не отображается"

**Причины:**
- Проблемы с загрузкой библиотеки `recharts`
- Неправильные данные о платежах

**Решение:**
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что данные о рассрочке приходят корректно через `/api/installment/my`
3. Проверьте, что `paid_amount` и `term_months` заполнены в БД

### Проблема: "Страница не найдена (404)"

**Причины:**
- Frontend контейнер не пересобран после изменений
- Неправильный URL

**Решение:**
1. Пересоберите frontend контейнер: `docker-compose build frontend && docker-compose up -d frontend`
2. Убедитесь, что открываете правильный URL (через nginx на `http://localhost`, а не напрямую порт)
3. Проверьте наличие файла страницы: `docker-compose exec frontend ls -la /app/app/auth/login/`

### Проблема: "База данных не подключается"

**Причины:**
- Неправильный `DATABASE_URL` в `.env`
- База данных не запущена
- Проблемы с сетью Docker

**Решение:**
1. Проверьте `DATABASE_URL` в `.env` файле
2. Убедитесь, что контейнер БД запущен: `docker-compose ps`
3. Проверьте логи БД: `docker-compose logs db`
4. Перезапустите все сервисы: `docker-compose restart`

---

## 📝 Логирование

Все важные операции логируются с разными уровнями важности.

### Просмотр логов

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f db

# Поиск ошибок
docker-compose logs backend | grep -i error

# Поиск платежей
docker-compose logs backend | grep -i payment

# Поиск запросов к Bitrix24
docker-compose logs backend | grep -i bitrix

# Поиск аутентификации
docker-compose logs backend | grep -i auth
```

### Уровни логирования

Уровень логирования настраивается через переменную `LOG_LEVEL` в `.env`:
- `DEBUG` - подробная информация для отладки
- `INFO` - общая информация о работе системы
- `WARNING` - предупреждения о потенциальных проблемах
- `ERROR` - только ошибки

---

## 🎯 Основные функции

✅ Аутентификация через magic link с JWT токенами  
✅ Проверка пользователя в Bitrix24 перед входом  
✅ Получение данных о рассрочке из Bitrix24 и локальной БД  
✅ Объединение данных из обоих источников (БД - источник истины для `paid_amount`)  
✅ Создание платежей через YooKassa  
✅ Обработка webhook от YooKassa  
✅ Обновление Bitrix24 при оплате (`UF_PAID_AMOUNT`)  
✅ Админ-панель для управления рассрочками  
✅ Внесение наличных платежей через админку  
✅ Изменение настроек рассрочки (срок, сумма, email, название)  
✅ Защита от race condition при одновременных платежах  
✅ Защита от дублирования платежей  
✅ Валидация всех данных  
✅ Логирование всех операций  
✅ График платежей и прогресс оплаты  

---

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи:** `docker-compose logs backend`
2. **Проверьте подключение к Bitrix24:** `/api/admin/bitrix/test` (требует авторизации)
3. **Проверьте настройки в `.env`:**
   - `BITRIX_WEBHOOK_URL` - правильный URL вебхука
   - `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET` - правильные данные
   - `DATABASE_URL` - правильное подключение к БД
4. **Убедитесь, что все сервисы запущены:** `docker-compose ps`
5. **Перезапустите сервисы:** `docker-compose restart`
